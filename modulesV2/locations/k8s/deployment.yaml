apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: persons-service
  name: persons-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: persons-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: persons-service
    spec:
      containers:
        - image: enkhalifapro/persons-service:latest
          imagePullPolicy: IfNotPresent
          name: persons-service
          resources:
            requests:
              cpu: 50m
              memory: 256Mi